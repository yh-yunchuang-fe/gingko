import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    indicator: {
        flexDirection: 'row',
        alignItems: 'center'
    },

    tip: {
        fontSize: 16,
        color: '#888888',
        marginLeft: 9
    }
})
